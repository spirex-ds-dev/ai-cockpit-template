# Preflight Review policy.
# Advisory-first by default. Enable gating explicitly if a repository wants
# status-based failures for selected Preflight Review states.

version: 1
gateEnabled: false
blockedStatuses: []
